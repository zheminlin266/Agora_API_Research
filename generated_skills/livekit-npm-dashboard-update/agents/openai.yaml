interface:
  display_name: "LiveKit npm Dashboard Update"
  short_description: "Update LiveKit npm weekly downloads CSV and dashboard."
  default_prompt: "Use $livekit-npm-dashboard-update to refresh the LiveKit npm downloads CSV/dashboard, validate it, and commit/push the root artifacts in Codex, then verify the public page."
