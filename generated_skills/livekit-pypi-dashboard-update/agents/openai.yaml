interface:
  display_name: "LiveKit PyPI Data Update"
  short_description: "Refresh LiveKit PyPI data for the single-page dashboard."
  default_prompt: "Use $livekit-pypi-dashboard-update to refresh the LiveKit PyPI CSV and metadata, validate the root dashboard, and report the result."
