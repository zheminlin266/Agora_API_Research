interface:
  display_name: "Agora PyPI Dashboard Update"
  short_description: "Update Agora PyPI weekly downloads CSV and dashboard."
  default_prompt: "Use $agora-pypi-dashboard-update to refresh the Agora PyPI downloads CSV/dashboard, validate it, and commit/push the root artifacts in Codex, then verify the public page."
